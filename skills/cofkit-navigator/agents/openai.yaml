interface:
  display_name: "COFkit Navigator"
  short_description: "Translate natural language into cofkit workflows"
  default_prompt: "Use $cofkit-navigator to turn my cofkit request into the right command or Python workflow."

policy:
  allow_implicit_invocation: true
